<% if @UserDetail.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@UserDetail.errors.count, "error") %>.
    </div>
    <ul>
    <% @UserDetail.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
